{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Relatorios</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Horadata</th>
                <td>{{ entity.horaData|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Evento</th>
                <td>{{ entity.evento }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('relatorios') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('relatorios_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
